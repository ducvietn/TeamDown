# ============================================================
# TeamUp Frontend — Example environment variables
# Copy this file to .env.local in the frontend/ directory.
# .env.local is already in frontend/.gitignore — it will NEVER be pushed.
# ============================================================

# Point to the backend API
VITE_API_URL=http://localhost:8080/api
